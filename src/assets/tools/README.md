# Tool assets

v0.0.0.3 keeps tool rendering lightweight and dependency-free. The gameplay toolbelt currently uses emoji labels; these SVG assets are ready for the next visual pass where each selected tool can render its dedicated icon.
